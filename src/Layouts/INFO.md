# res://Themes/

This directory contains the default theme ("UMP_DEFAULT") & is where custom themes loaded via the Tesseract mod loader get mounted to in the virtual file system.
Adding directories directly will do nothing, you must load themes through Tesseract or modify the default theme.
