- Add playlists.
- Add auto scan for changes in library.
- Fix queue not being updated properly when reordering an item BEFORE the currently playing track.
- Add right-click options for artist, album, & genre cards.
- Add ability to upload custom artist & album covers.
- Add simple mini-player to replace immersive player.
- Revamp library "scan for changes" implementation to only call CLI once, not for every file.
- Add status info for "scan for changes" function.
- Revamp "interface.py" CLI to return raw image bytes as a string instead of storing as a file.
- Add option for artist/album for uploading custom image.
- Add option for artist for regrabbing artist image from API.

- Optimize context menu creation. Dont create new instance for EVERY track listed in queue & tabs. Use a singular global instance that takes data from the caller.


- Build GDExtension for dedicated audio playback & metadata.
  This is to address the problems of needing to use a bundled Python executable for metadata & FLACs not supporting multi-channel.
